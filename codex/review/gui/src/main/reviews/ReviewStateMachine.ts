import type { ReviewStatus } from "../../shared/contracts";
const transitions: Record<ReviewStatus, ReviewStatus[]> = { not_configured: ["ready"], blocked_requirements: ["ready"], ready: ["queued", "starting"], blocked_dirty: ["ready"], blocked_git_operation: ["ready"], queued: ["starting", "interrupted"], starting: ["reviewing", "interrupted", "failed", "recovering"], reviewing: ["completed", "interrupted", "failed", "stale", "recovering", "waiting_for_approval"], waiting_for_approval: ["reviewing", "interrupted", "failed"], completed: ["stale", "queued", "starting"], interrupted: ["queued", "starting"], failed: ["queued", "starting"], stale: ["queued", "starting"], recovering: ["reviewing", "completed", "interrupted", "failed", "stale"] };
export function canTransition(from: ReviewStatus, to: ReviewStatus) { return transitions[from]?.includes(to) ?? false; }
